;;; et-keys.el --- Set misc. custom keybindings -*- lexical-binding: t; -*-
;;; Commentary:
;;; Code:

(use-package which-key
  :config
  (which-key-mode 1))

(provide 'et-keys)
;;; et-keys.el ends here
